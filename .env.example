NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_ADMIN_WHATSAPP=

# Canonical site origin for Open Graph / metadata (no trailing slash).
# Defaults to https://digica-academy.web.id when unset.
# NEXT_PUBLIC_SITE_URL=https://digica-academy.web.id

# Auth emails (signup confirm + password reset) require custom SMTP in the
# Supabase dashboard. Built-in mail is ~2/hour and will block production.
# See supabase/README.md → Production auth email.
